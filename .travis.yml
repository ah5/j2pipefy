language: groovy

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock

install: 
  - pip install .
  - pip install coveralls

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

after_sucess:
  coveralls